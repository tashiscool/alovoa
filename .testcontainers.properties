# Testcontainers configuration for CI
# Enable reuse to speed up tests when running locally
testcontainers.reuse.enable=true

# Disable ryuk in CI environments where Docker is ephemeral
ryuk.container.privileged=true
